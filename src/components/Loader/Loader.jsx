import './loader.css'

const Loader = () => {
    return (
        <div className="loader-container">
            <span className="loading loading-ring loading-lg bg-[#FC8934]"></span>
        </div>
    );
};

export default Loader;
